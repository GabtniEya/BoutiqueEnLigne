@{
    ViewData["Title"] = "Accueil";
}

<style>
    @@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;800;900&family=Inter:wght@300;400;500;600;700;800&display=swap');

    :root {
        --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        --secondary-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        --dark-gradient: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
        --gold-gradient: linear-gradient(135deg, #ffd700 0%, #ffed4e 100%);
    }

    * {
        font-family: 'Inter', sans-serif;
    }

    body {
        overflow-x: hidden;
    }

    /* Hero Section Ultra Premium */
    .hero-section {
        position: relative;
        background: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
        padding: 140px 0 120px;
        overflow: hidden;
        margin-bottom: 100px;
    }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 20% 50%, rgba(102, 126, 234, 0.15) 0%, transparent 50%), radial-gradient(circle at 80% 80%, rgba(139, 92, 246, 0.15) 0%, transparent 50%);
            animation: pulse 8s ease-in-out infinite;
        }

    @@keyframes pulse {
        0%, 100% {
            opacity: 1;
        }

        50% {
            opacity: 0.8;
        }
    }

    .hero-section::after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M11 18c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm48 25c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm-43-7c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm63 31c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3z' fill='%23ffffff' fill-opacity='0.03' fill-rule='evenodd'/%3E%3C/svg%3E");
        opacity: 0.5;
    }

    .hero-content {
        position: relative;
        z-index: 2;
    }

    .hero-badge {
        display: inline-block;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        padding: 12px 28px;
        border-radius: 50px;
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: #fff;
        font-size: 0.9rem;
        font-weight: 600;
        margin-bottom: 30px;
        animation: fadeInUp 0.8s ease;
        letter-spacing: 0.5px;
    }

    .hero-section h1 {
        font-family: 'Playfair Display', serif;
        font-weight: 900;
        font-size: 4.5rem;
        letter-spacing: -2px;
        margin-bottom: 28px;
        animation: fadeInUp 0.8s ease 0.1s backwards;
        background: linear-gradient(135deg, #fff 0%, #e0e7ff 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1.1;
    }

    .hero-section .lead {
        font-size: 1.5rem;
        font-weight: 400;
        color: rgba(255, 255, 255, 0.85);
        animation: fadeInUp 0.8s ease 0.2s backwards;
        max-width: 700px;
        margin: 0 auto 50px;
        line-height: 1.6;
    }

    .hero-cta-buttons {
        display: flex;
        gap: 20px;
        justify-content: center;
        margin-bottom: 60px;
        animation: fadeInUp 0.8s ease 0.3s backwards;
    }

    .btn-hero-primary {
        background: var(--gold-gradient);
        color: #1e293b;
        border: none;
        padding: 18px 45px;
        border-radius: 50px;
        font-weight: 700;
        font-size: 1.05rem;
        transition: all 0.3s ease;
        box-shadow: 0 10px 30px rgba(255, 215, 0, 0.4);
    }

        .btn-hero-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(255, 215, 0, 0.5);
            color: #0f172a;
        }

    .btn-hero-secondary {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        color: #fff;
        border: 2px solid rgba(255, 255, 255, 0.3);
        padding: 16px 40px;
        border-radius: 50px;
        font-weight: 600;
        font-size: 1.05rem;
        transition: all 0.3s ease;
    }

        .btn-hero-secondary:hover {
            background: rgba(255, 255, 255, 0.2);
            border-color: rgba(255, 255, 255, 0.5);
            transform: translateY(-3px);
            color: #fff;
        }

    .hero-stats {
        display: flex;
        justify-content: center;
        gap: 80px;
        animation: fadeInUp 0.8s ease 0.4s backwards;
    }

    .hero-stat {
        text-align: center;
        position: relative;
    }

        .hero-stat::after {
            content: '';
            position: absolute;
            right: -40px;
            top: 50%;
            transform: translateY(-50%);
            width: 1px;
            height: 60px;
            background: rgba(255, 255, 255, 0.2);
        }

        .hero-stat:last-child::after {
            display: none;
        }

    .hero-stat-number {
        display: block;
        font-family: 'Playfair Display', serif;
        font-size: 3.2rem;
        font-weight: 900;
        background: var(--gold-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1;
        margin-bottom: 10px;
    }

    .hero-stat-label {
        font-size: 1rem;
        color: rgba(255, 255, 255, 0.8);
        font-weight: 500;
        letter-spacing: 0.5px;
    }

    @@keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(40px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Section Headers */
    .section-header {
        text-align: center;
        margin-bottom: 80px;
    }

    .section-badge {
        display: inline-block;
        background: var(--primary-gradient);
        color: #fff;
        padding: 8px 24px;
        border-radius: 50px;
        font-size: 0.85rem;
        font-weight: 600;
        margin-bottom: 20px;
        letter-spacing: 1px;
        text-transform: uppercase;
    }

    .section-header h2 {
        font-family: 'Playfair Display', serif;
        font-weight: 900;
        font-size: 3.5rem;
        color: #0f172a;
        margin-bottom: 20px;
        letter-spacing: -1px;
    }

    .section-header p {
        color: #64748b;
        font-size: 1.2rem;
        max-width: 650px;
        margin: 0 auto;
        font-weight: 400;
        line-height: 1.7;
    }

    /* Premium Category Cards */
    .category-card {
        position: relative;
        border: none;
        border-radius: 30px;
        overflow: hidden;
        height: 100%;
        background: #fff;
        transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        box-shadow: 0 8px 30px rgba(0,0,0,0.06);
    }

        .category-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 8px;
            background: var(--primary-gradient);
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.5s ease;
        }

        .category-card::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.03) 0%, rgba(139, 92, 246, 0.03) 100%);
            opacity: 0;
            transition: opacity 0.5s ease;
        }

        .category-card:hover {
            transform: translateY(-16px);
            box-shadow: 0 30px 70px rgba(0,0,0,0.12);
        }

            .category-card:hover::before {
                transform: scaleX(1);
            }

            .category-card:hover::after {
                opacity: 1;
            }

        .category-card .card-body {
            padding: 55px 40px;
            background: transparent;
            position: relative;
            z-index: 1;
        }

    .category-icon-wrapper {
        width: 110px;
        height: 110px;
        margin: 0 auto 30px;
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        border-radius: 28px;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.4s ease;
        box-shadow: 0 8px 25px rgba(0,0,0,0.08);
    }

    .category-card:hover .category-icon-wrapper {
        background: var(--primary-gradient);
        transform: translateY(-10px) rotate(-5deg);
        box-shadow: 0 15px 40px rgba(102, 126, 234, 0.4);
    }

    .category-icon {
        font-size: 5rem;
        transition: all 0.4s ease;
        display: block;
    }

    .category-card:hover .category-icon {
        filter: brightness(0) invert(1);
        transform: scale(1.1);
    }

    .category-card .card-title {
        font-family: 'Playfair Display', serif;
        font-weight: 800;
        font-size: 1.8rem;
        margin-bottom: 18px;
        color: #0f172a;
        letter-spacing: -0.5px;
    }

    .category-card .card-text {
        color: #64748b;
        margin-bottom: 32px;
        font-size: 1.05rem;
        line-height: 1.7;
    }

    .btn-category {
        background: var(--primary-gradient);
        border: none;
        padding: 16px 40px;
        border-radius: 50px;
        font-weight: 700;
        font-size: 1rem;
        letter-spacing: 0.5px;
        transition: all 0.3s ease;
        box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
        position: relative;
        overflow: hidden;
        color: #fff;
    }

        .btn-category::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
            transition: left 0.6s ease;
        }

        .btn-category:hover {
            transform: scale(1.08);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.4);
            color: #fff;
        }

            .btn-category:hover::before {
                left: 100%;
            }

    /* Luxury Features Section */
    .features-section {
        background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
        padding: 100px 0;
        margin: 100px 0;
        border-radius: 0;
        position: relative;
        overflow: hidden;
    }

        .features-section::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(102, 126, 234, 0.15) 0%, transparent 70%);
            border-radius: 50%;
        }

        .features-section::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(139, 92, 246, 0.15) 0%, transparent 70%);
            border-radius: 50%;
        }

        .features-section .section-header h2 {
            color: #fff;
        }

        .features-section .section-header p {
            color: rgba(255, 255, 255, 0.8);
        }

    .feature-box {
        text-align: center;
        padding: 50px 35px;
        transition: all 0.4s ease;
        position: relative;
        z-index: 1;
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(10px);
        border-radius: 30px;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

        .feature-box:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-10px);
            border-color: rgba(255, 255, 255, 0.2);
        }

    .feature-icon-wrapper-alt {
        width: 120px;
        height: 120px;
        margin: 0 auto 35px;
        background: var(--gold-gradient);
        border-radius: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 15px 40px rgba(255, 215, 0, 0.3);
        transition: all 0.4s ease;
    }

    .feature-box:hover .feature-icon-wrapper-alt {
        transform: translateY(-12px) rotate(-5deg);
        box-shadow: 0 20px 50px rgba(255, 215, 0, 0.5);
    }

    .feature-icon-alt {
        font-size: 3.5rem;
        filter: brightness(0);
    }

    .feature-box h4 {
        font-family: 'Playfair Display', serif;
        font-weight: 800;
        color: #fff;
        margin-bottom: 18px;
        font-size: 1.6rem;
    }

    .feature-box p {
        color: rgba(255, 255, 255, 0.8);
        margin: 0;
        font-size: 1.1rem;
        line-height: 1.7;
    }

    /* Premium CTA Section */
    .cta-section {
        text-align: center;
        margin: 100px 0 80px;
        padding: 80px 60px;
        background: var(--primary-gradient);
        border-radius: 40px;
        position: relative;
        overflow: hidden;
        box-shadow: 0 30px 60px rgba(102, 126, 234, 0.3);
    }

        .cta-section::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.15) 0%, transparent 70%);
            animation: rotate 25s linear infinite;
        }

    @@keyframes rotate {
        from {
            transform: rotate(0deg);
        }

        to {
            transform: rotate(360deg);
        }
    }

    .cta-content {
        position: relative;
        z-index: 2;
    }

    .cta-section h3 {
        font-family: 'Playfair Display', serif;
        color: #fff;
        font-weight: 900;
        font-size: 3rem;
        margin-bottom: 22px;
        letter-spacing: -1px;
    }

    .cta-section p {
        color: rgba(255,255,255,0.95);
        font-size: 1.3rem;
        margin-bottom: 45px;
        font-weight: 400;
    }

    .btn-cta {
        background: #fff;
        color: #667eea;
        border: none;
        padding: 22px 65px;
        font-size: 1.2rem;
        border-radius: 50px;
        font-weight: 800;
        transition: all 0.4s ease;
        box-shadow: 0 20px 50px rgba(0,0,0,0.2);
        position: relative;
        overflow: hidden;
    }

        .btn-cta::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(102, 126, 234, 0.1);
            transform: translate(-50%, -50%);
            transition: width 0.7s, height 0.7s;
        }

        .btn-cta:hover::before {
            width: 350px;
            height: 350px;
        }

        .btn-cta:hover {
            transform: translateY(-8px);
            box-shadow: 0 25px 60px rgba(0,0,0,0.3);
            color: #5a67d8;
        }

        .btn-cta span {
            position: relative;
            z-index: 1;
            letter-spacing: 0.5px;
        }

    /* Responsive */
    @@media (max-width: 768px) {
        .hero-section h1 {
            font-size: 2.8rem;
        }

        .hero-stats {
            flex-direction: column;
            gap: 40px;
        }

        .hero-stat::after {
            display: none;
        }

        .section-header h2 {
            font-size: 2.2rem;
        }

        .category-card .card-body {
            padding: 45px 30px;
        }

        .hero-cta-buttons {
            flex-direction: column;
        }

        .cta-section {
            padding: 60px 30px;
        }
    }
</style>

<!-- Hero Section -->
<div class="hero-section">
    <div class="container">
        <div class="hero-content text-center text-white">
            <div class="hero-badge">✨ Boutique Premium - Collection 2025</div>
            <h1>L'Excellence au<br>Service du Style</h1>
            <p class="lead">Découvrez notre univers exclusif où qualité exceptionnelle et design sophistiqué se rencontrent pour sublimer votre quotidien</p>

            <div class="hero-cta-buttons">
                <a asp-controller="Produits" asp-action="Index" class="btn btn-hero-primary">
                    Découvrir la Collection
                </a>
                <a href="#categories" class="btn btn-hero-secondary">
                    Explorer les Catégories
                </a>
            </div>

            <div class="hero-stats">
                <div class="hero-stat">
                    <span class="hero-stat-number">10K+</span>
                    <span class="hero-stat-label">PRODUITS PREMIUM</span>
                </div>
                <div class="hero-stat">
                    <span class="hero-stat-number">25K+</span>
                    <span class="hero-stat-label">CLIENTS CONQUIS</span>
                </div>
                <div class="hero-stat">
                    <span class="hero-stat-number">99.8%</span>
                    <span class="hero-stat-label">SATISFACTION</span>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Section Catégories -->
<div class="container" id="categories">
    <div class="section-header">
        <div class="section-badge">Nos Collections</div>
        <h2>Explorez l'Excellence</h2>
        <p>Chaque catégorie a été soigneusement sélectionnée pour vous offrir le meilleur du luxe accessible et de la qualité premium</p>
    </div>

    <div class="row g-5 mb-5">
        <div class="col-lg-4 col-md-6">
            <div class="card category-card">
                <div class="card-body text-center">
                    <div class="category-icon-wrapper">
                        <span class="category-icon">📱</span>
                    </div>
                    <h3 class="card-title">Électronique</h3>
                    <p class="card-text">Les innovations technologiques les plus avancées et les gadgets intelligents qui redéfinissent votre quotidien</p>
                    <a asp-controller="Produits" asp-action="Index" asp-route-categorieId="1" class="btn btn-primary btn-category">Explorer la Collection</a>
                </div>
            </div>
        </div>

        <div class="col-lg-4 col-md-6">
            <div class="card category-card">
                <div class="card-body text-center">
                    <div class="category-icon-wrapper">
                        <span class="category-icon">👕</span>
                    </div>
                    <h3 class="card-title">Vêtements</h3>
                    <p class="card-text">Collections haute couture et prêt-à-porter raffiné qui allient élégance contemporaine et confort absolu</p>
                    <a asp-controller="Produits" asp-action="Index" asp-route-categorieId="2" class="btn btn-primary btn-category">Explorer la Collection</a>
                </div>
            </div>
        </div>

        <div class="col-lg-4 col-md-6">
            <div class="card category-card">
                <div class="card-body text-center">
                    <div class="category-icon-wrapper">
                        <span class="category-icon">📚</span>
                    </div>
                    <h3 class="card-title">Livres</h3>
                    <p class="card-text">Une bibliothèque d'excellence avec des œuvres soigneusement choisies pour enrichir votre esprit et votre culture</p>
                    <a asp-controller="Produits" asp-action="Index" asp-route-categorieId="3" class="btn btn-primary btn-category">Explorer la Collection</a>
                </div>
            </div>
        </div>

        <div class="col-lg-4 col-md-6">
            <div class="card category-card">
                <div class="card-body text-center">
                    <div class="category-icon-wrapper">
                        <span class="category-icon">🏠</span>
                    </div>
                    <h3 class="card-title">Maison</h3>
                    <p class="card-text">Mobilier design et décoration d'intérieur haut de gamme pour transformer votre espace en havre de paix</p>
                    <a asp-controller="Produits" asp-action="Index" asp-route-categorieId="4" class="btn btn-primary btn-category">Explorer la Collection</a>
                </div>
            </div>
        </div>

        <div class="col-lg-4 col-md-6">
            <div class="card category-card">
                <div class="card-body text-center">
                    <div class="category-icon-wrapper">
                        <span class="category-icon">👟</span>
                    </div>
                    <h3 class="card-title">Chaussures</h3>
                    <p class="card-text">Confort premium et designs avant-gardistes pour accompagner chacun de vos pas avec style et assurance</p>
                    <a asp-controller="Produits" asp-action="Index" asp-route-categorieId="5" class="btn btn-primary btn-category">Explorer la Collection</a>
                </div>
            </div>
        </div>

        <div class="col-lg-4 col-md-6">
            <div class="card category-card">
                <div class="card-body text-center">
                    <div class="category-icon-wrapper">
                        <span class="category-icon">🌸</span>
                    </div>
                    <h3 class="card-title">Parfums</h3>
                    <p class="card-text">Fragrances d'exception et signatures olfactives exclusives créées par les plus grands nez de la parfumerie</p>
                    <a asp-controller="Produits" asp-action="Index" asp-route-categorieId="6" class="btn btn-primary btn-category">Explorer la Collection</a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Section Avantages -->
<div class="features-section">
    <div class="container">
        <div class="section-header">
            <div class="section-badge">Nos Engagements</div>
            <h2>L'Excellence à Votre Service</h2>
            <p>Nous redéfinissons les standards du e-commerce avec un service irréprochable et une attention méticuleuse à chaque détail</p>
        </div>

        <div class="row g-5">
            <div class="col-lg-4 col-md-12">
                <div class="feature-box">
                    <div class="feature-icon-wrapper-alt">
                        <span class="feature-icon-alt">⚡</span>
                    </div>
                    <h4>Livraison Express Premium</h4>
                    <p>Service de livraison ultra-rapide en 24-48h avec emballage soigné et suivi GPS en temps réel pour une tranquillité absolue</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-12">
                <div class="feature-box">
                    <div class="feature-icon-wrapper-alt">
                        <span class="feature-icon-alt">🔐</span>
                    </div>
                    <h4>Sécurité Maximale</h4>
                    <p>Cryptage SSL de niveau bancaire et protection multicouche de vos données personnelles et bancaires avec certification PCI DSS</p>
                </div>
            </div>

            <div class="col-lg-4 col-md-12">
                <div class="feature-box">
                    <div class="feature-icon-wrapper-alt">
                        <span class="feature-icon-alt">👑</span>
                    </div>
                    <h4>Service Client d'Excellence</h4>
                    <p>Équipe dédiée disponible 7j/7, retours gratuits sous 30 jours et garantie satisfait ou remboursé sans condition pour votre sérénité</p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Call to Action -->
<div class="container">
    <div class="cta-section">
        <div class="cta-content">
            <h3>Rejoignez l'Excellence</h3>
            <p>Plus de 10,000 produits premium vous attendent dans notre catalogue exclusif</p>
            <a asp-controller="Produits" asp-action="Index" class="btn btn-cta">
                <span>Découvrir Notre Univers</span>
            </a>
        </div>
    </div>
</div>